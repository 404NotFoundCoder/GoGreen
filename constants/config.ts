/** App 層級設定（對應 INSTRUCTIONS.md） */

/** 排行榜主列表每頁筆數（超過則分頁） */
export const LEADERBOARD_LIMIT = 20;

export const MAX_CUSTOM_ITEMS = 10; // 每日可加入今日清單的自訂項目上限

export const MAX_FAVORITE_ITEMS = 20; // 常用清單收藏上限

/** 單一公版／自訂項目、單日打卡最多佐證張數 */
export const MAX_CHECKIN_PHOTOS = 10;

export const CUSTOM_ITEM_MAX_LENGTH = 40; // 自訂項目文字長度上限

/** 每日備註（與項目無關）字數上限 */
export const MAX_DAY_NOTE_LENGTH = 2000;

/** 每日重置時間（UTC+8 的 0 點） */
export const DAILY_RESET_HOUR = 0;

export const INVITE_CODE_LENGTH = 6; // 群組邀請碼長度

export const TIMEZONE = "Asia/Taipei";
